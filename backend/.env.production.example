# IMPORTANTE: Este archivo es solo un ejemplo
# En Railway, configura estas variables de entorno en el dashboard

# Server
PORT=4000
NODE_ENV=production

# Database (Railway te proporcionará esta URL automáticamente)
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# JWT (GENERA UNA CLAVE SEGURA NUEVA)
JWT_SECRET="CAMBIA_ESTO_POR_UNA_CLAVE_SUPER_SEGURA_Y_LARGA"
JWT_EXPIRES_IN="7d"

# CORS (Agrega tu dominio de Vercel aquí)
ALLOWED_ORIGINS="https://tu-app.vercel.app,https://www.tu-dominio.com"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
